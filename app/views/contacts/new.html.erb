<% content_for(:title, 'New Contact | Wikipages!') %>
<body background="/assets/swirl_pattern.png">
	<div class="container">
		<div class="page-header">
			<h1>New Contact</h1> 
			<h3><small>Enter your contact information below:</small></h3>
		</div>
		<%= render('layouts/errors', :object => @contact) %>
		<%= render('form') %>
	</div>	
</body>